#include <stdio.h>
#include <stdlib.h>
#include <string.h>
#include<MyLibrary.h>
int main()





/*
{   char ch,ch2;

do{
    printf("enter a character\n");
    ch = getche();
    if (ch == -32)
    {
        printf("its extended key ");
        ch2=getche();
        printf("the ascii value is %d",ch2);
     }
     else
       {
        printf("\nits normal key\n");
        printf("the ascii value is %d\n",ch);
       }
}while(ch!=27);
*/
     ///the first program
   /* char fname[10],lname[10],fullname[20];
    printf("enter the first name\n");
    gets(fname);
    printf("enter the last name\n");
    gets(lname);
    strcpy(fullname,fname);
    strcat(fullname," ");
    strcat(fullname,lname);
    printf("\n hello %s \n",fullname);
    */

    return 0;
}
